# SPDX-FileCopyrightText: 2026 Jiri Vyskocil
# SPDX-License-Identifier: Apache-2.0

"""Adapter for the ``terok_sandbox`` wheel (and its re-exports of shield).

Re-exports every symbol terok consumes from terok-sandbox.  Callers
elsewhere in terok import from this module rather than from
``terok_sandbox`` directly — see the package docstring in
[`terok.lib.integrations`][terok.lib.integrations] for the rationale.

terok-shield doesn't have its own adapter: the only domain-side access
to shield is through sandbox's re-exports (``make_shield``, ``up``,
``down``).  The CLI command bridge in ``terok.cli.commands.shield``
that wraps shield's own CLI registry is the lone exception and imports
``terok_shield.*`` directly under a narrow allowed-importer rule.
"""

from terok_sandbox import (  # noqa: F401 — re-exported public API
    SERVICES_TCP_OPTOUT_YAML,
    BestEffortLogger,
    ConfigScope,
    ConfigStack,
    ContainerRuntime,
    EnvironmentCheck,
    ExecResult,
    GateServerManager,
    GateServerStatus,
    GateStalenessInfo,
    GitGate,
    LifecycleHooks,
    NullRuntime,
    PodmanRuntime,
    RawSSHSection,
    Sandbox,
    SandboxConfig,
    ServicesMode,
    SetupVerdict,
    Sharing,
    SSHManager,
    VaultManager,
    VaultStatus,
    VolumeSpec,
    bold,
    check_environment,
    check_units_outdated,
    claim_port,
    create_token,
    down,
    ensure_server_reachable,
    gate_use_personal_ssh_default,
    get_gate_base_path,
    get_gate_server_port,
    get_server_status,
    get_ssh_signer_port,
    get_token_broker_port,
    get_vault_status,
    is_systemd_available,
    is_vault_socket_active,
    is_vault_systemd_available,
    make_shield,
    needs_setup,
    red,
    release_port,
    resolve_container_state_dir,
    stage_line,
    start_daemon,
    start_vault,
    stop_daemon,
    stop_vault,
    up,
    yellow,
)
from terok_sandbox.credentials.encryption import (  # noqa: F401 — re-exported public API
    NoPassphraseError,
)
from terok_sandbox.doctor import (  # noqa: F401 — re-exported public API
    CheckVerdict,
    DoctorCheck,
    sandbox_doctor_checks,
)

__all__ = [
    "BestEffortLogger",
    "CheckVerdict",
    "ConfigScope",
    "ConfigStack",
    "ContainerRuntime",
    "DoctorCheck",
    "EnvironmentCheck",
    "ExecResult",
    "GateServerManager",
    "GateServerStatus",
    "GateStalenessInfo",
    "GitGate",
    "LifecycleHooks",
    "NoPassphraseError",
    "NullRuntime",
    "PodmanRuntime",
    "RawSSHSection",
    "SERVICES_TCP_OPTOUT_YAML",
    "SSHManager",
    "Sandbox",
    "SandboxConfig",
    "ServicesMode",
    "SetupVerdict",
    "Sharing",
    "VaultManager",
    "VaultStatus",
    "VolumeSpec",
    "bold",
    "check_environment",
    "check_units_outdated",
    "claim_port",
    "create_token",
    "down",
    "ensure_server_reachable",
    "gate_use_personal_ssh_default",
    "get_gate_base_path",
    "get_gate_server_port",
    "get_server_status",
    "get_ssh_signer_port",
    "get_token_broker_port",
    "get_vault_status",
    "is_systemd_available",
    "is_vault_socket_active",
    "is_vault_systemd_available",
    "make_shield",
    "needs_setup",
    "red",
    "release_port",
    "resolve_container_state_dir",
    "sandbox_doctor_checks",
    "stage_line",
    "start_daemon",
    "start_vault",
    "stop_daemon",
    "stop_vault",
    "up",
    "yellow",
]
